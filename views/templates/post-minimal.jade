.row
	.col-xs-10
		h3.text-overflow 
			a(href='{{post.alternate.href}}',target='_blank') {{post.title}}
		h4 {{post.published}} &nbsp;//&nbsp;
			a(href='',ng-click='toggle(post)') Expand post
	.col-xs-2
		a(href='{{post.alternate.href}}',target='_blank')
			img(ng-src="{{post.content.images.large.url}}",style="max-width:100%;height:auto;margin-bottom:10px;")
.row
	.col-xs-12
		.line-separator
