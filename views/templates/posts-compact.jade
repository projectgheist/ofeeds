include post-nav
.list-article-group
	.article.article-sm(ng-repeat="post in stream.items",on-last-repeat='')
		.row(id='{{post.id}}')
			a(href='',ng-click="toggle(post.id)")
				.col-xs-9
					p.text-overflow {{post.title}}
				.col-xs-3(style="text-align:right")
					p {{post.published}}
		.line-separator